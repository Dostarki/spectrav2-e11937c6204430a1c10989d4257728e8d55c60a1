import { ZKProofData } from './types';
/**
 * Initializes the WASM module. Must be called before using proof functions.
 *
 * @param wasmUrl - (Browser only) Optional custom URL to the WASM file.
 *                  If not provided, will try default paths: /wasm/settler_wasm_bg.wasm,
 *                  ./wasm/settler_wasm_bg.wasm, ../wasm/settler_wasm_bg.wasm
 *
 * @example
 * // Node.js
 * await initWASM();
 *
 * @example
 * // Browser with default path
 * await initWASM();
 *
 * @example
 * // Browser with custom path
 * await initWASM('/public/wasm/settler_wasm_bg.wasm');
 */
export declare function initWASM(wasmUrl?: string): Promise<void>;
export declare function generateRangeProof(amount: number, bitLength?: number): Promise<ZKProofData>;
export declare function verifyRangeProof(proofBytes: string, commitmentBytes: string, bitLength?: number): Promise<boolean>;
export declare function isWASMSupported(): boolean;
//# sourceMappingURL=zkProofs.d.ts.map